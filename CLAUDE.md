# CLAUDE.md

This project uses `AGENTS.md` as its canonical agent entry point. Read it first, then read every document it requires before writing code. If a referenced file is missing or contradicts the implementation, stop and report the mismatch.
